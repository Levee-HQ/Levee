[workspace]
resolver = "2"
members = [
    "contracts/levee-registry",
    "contracts/levee-pool",
    "contracts/levee-policy",
    "contracts/levee-oracle",
    "contracts/levee-settlement",
    "contracts/shared",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
repository = "https://github.com/Levee-HQ/Levee"
authors = ["Levee Contributors"]

[workspace.dependencies]
soroban-sdk = "27.0.5"

[profile.release]
opt-level = "z"
overflow-checks = true
debug = 0
strip = "symbols"
debug-assertions = false
panic = "abort"
codegen-units = 1
lto = true

[profile.release-with-logs]
inherits = "release"
debug-assertions = true
